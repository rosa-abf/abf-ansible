---

- name: create user rosa
  user: state=present name=rosa createhome=yes shell=/bin/bash

- name: configure sudoers
  lineinfile: "dest=/etc/sudoers backup=yes state=present regexp='^%rosa' line='%rosa ALL=(ALL) NOPASSWD: ALL'"
  tags: config-sudoers

- include: access.yml
